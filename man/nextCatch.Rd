% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pid.R
\name{nextCatch}
\alias{nextCatch}
\title{Next-year catch from a PID-style HCR}
\usage{
nextCatch(
  bstate,
  fstate,
  btrnd,
  ftrnd,
  aucBstate,
  aucFstate,
  aucBtrnd,
  aucFtrnd,
  ent,
  CNow,
  lambdaBstate = 1,
  lambdaFstate = 1,
  lambdaBtrnd = 1,
  lambdaFtrnd = 1,
  Kp = 0.5,
  Kd = 0,
  entLow = 0,
  entHigh = 1,
  gMin = 0.2,
  maxChange = 0.3
)
}
\arguments{
\item{bstate}{Numeric. Biomass state (log-ratio to target).}

\item{fstate}{Numeric. Fishing mortality state (log-ratio to target).}

\item{btrnd}{Numeric. Biomass trend (slope).}

\item{ftrnd}{Numeric. F trend (slope).}

\item{aucBstate}{Numeric. AUC (0.5-1) for bstate indicator quality.}

\item{aucFstate}{Numeric. AUC for fstate indicator quality.}

\item{aucBtrnd}{Numeric. AUC for btrnd indicator quality.}

\item{aucFtrnd}{Numeric. AUC for ftrnd indicator quality.}

\item{ent}{Numeric. Entropy (0-1) of biomass time series.}

\item{CNow}{Numeric. Current year's catch.}

\item{lambdaBstate}{Numeric (default 1.0). Multiplier on bstate weight.}

\item{lambdaFstate}{Numeric (default 1.0). Multiplier on fstate weight.}

\item{lambdaBtrnd}{Numeric (default 1.0). Multiplier on btrnd weight.}

\item{lambdaFtrnd}{Numeric (default 1.0). Multiplier on ftrnd weight.}

\item{Kp}{Numeric (default 0.5). Proportional gain.}

\item{entLow}{Numeric (default 0.0). Lower entropy bound.}

\item{entHigh}{Numeric (default 1.0). Upper entropy bound.}

\item{gMin}{Numeric (default 0.2). Minimum gain at high entropy.}

\item{maxChange}{Numeric (default 0.3). Max ± annual TAC change (30%).}
}
\value{
Numeric. Next year's catch, constrained by maxChange.
}
\description{
Calculates the next year's TAC/catch based on four indicators
  (biomass state, Fstate, trends) using AUC-weighted composite error and
  entropy-based damping.
}
\details{
The rule constructs a composite error E from the four indicators,
  weighted by lambda_i * max(0, AUC_i - 0.5). Entropy dampens the
  proportional response via a linear gain g_ent between 1 (low entropy)
  and gMin (high entropy). The update is multiplicative on log-scale:
  C_next=C_now * exp( g_ent * Kp * E ), clipped to [C_now*(1-maxChange),
  C_now*(1+maxChange)].
}
