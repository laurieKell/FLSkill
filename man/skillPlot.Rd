% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/plots.R
\name{skillPlot}
\alias{skillPlot}
\alias{skillPlot,ANY-method}
\alias{skillPlot_old}
\title{Skill Plot}
\usage{
skillPlot(obs, pred, ...)

\S4method{skillPlot}{ANY}(
  obs,
  pred,
  data = NULL,
  group = NULL,
  threshold = 1,
  reference = 1,
  xLabel = "",
  limits = c(0, 5),
  labels = c("Observed", "Predicted"),
  panels = c("density", "scatter", "roc"),
  logScale = TRUE,
  maxPoints = 1000,
  themeStyle = "bw"
)

skillPlot_old(
  data,
  obs,
  hat,
  threshold = 1,
  reference = 1,
  xLabel = "",
  limits = c(0, 5),
  labels = c(obs, hat),
  group = NULL,
  ...
)
}
\arguments{
\item{obs}{Character name of column with true stock status, or numeric vector}

\item{pred}{Character name of column with predicted status, or numeric vector}

\item{...}{Additional arguments passed to methods (data, group, threshold, reference, panels, etc.)}

\item{data}{Data.frame containing assessment results (optional if obs/pred are vectors)}

\item{group}{Character name of grouping column (e.g., "Scenario"). If NULL, automatically uses "Scenario" if present, otherwise no grouping.}

\item{threshold}{Numeric threshold for classification (default=1)}

\item{reference}{Reference value for status classification (default=1)}

\item{xLabel}{Axis label for plots (default="")}

\item{limits}{X-axis limits for density plots (default=c(0,5))}

\item{labels}{Character vector of length 2 for observed/predicted labels (default=c("Observed", "Predicted"))}

\item{panels}{Character vector specifying which panels to show: "density", "scatter", "roc" (default: all three)}

\item{logScale}{Logical, use log scale for scatter plot (default=TRUE)}

\item{maxPoints}{Integer, maximum number of points to plot in scatter (default=1000)}

\item{themeStyle}{Character, ggplot2 theme style: "bw", "minimal", "classic" (default="bw")}

\item{hat}{Character name of column with predicted status (old parameter name, use 'pred' in new signature)}
}
\value{
ggplot object with diagnostic visualization panels

ggplot object with diagnostic visualization panels
}
\description{
Generates flexible diagnostic plots for management procedure evaluation.
Supports multiple input types (numeric vectors, data.frame columns, FLQuants) and 
customizable visualization panels (density, scatter, ROC curves).
}
\examples{
set.seed(123)
obs  = rlnorm(200, meanlog=log(1), sdlog=0.5)
df = data.frame(Scenario = rep(1:2, each=100),
                 obs  = obs,
                 pred = obs*c(rlnorm(100, meanlog=log(1), sdlog=0.2),
                              rlnorm(100, meanlog=log(1), sdlog=0.5)*1.5))
                              
# Using column names with data.frame
skillPlot(obs="obs", pred="pred", data=df)

# Using numeric vectors directly
skillPlot(obs=df$obs, pred=df$pred)

# Custom panels
skillPlot(obs="obs", pred="pred", data=df, panels=c("scatter", "roc"))

# Custom grouping
skillPlot(obs="obs", pred="pred", data=df, group="Scenario")
}
\keyword{internal}
