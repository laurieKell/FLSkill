% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/classification.R
\name{skillScore}
\alias{skillScore}
\alias{skillScore,logical,numeric-method}
\title{Skill Score Calculation}
\usage{
skillScore(
  response,
  predictor,
  reference = NULL,
  threshold = 1,
  ci = FALSE,
  ciLevel = 0.95,
  nBoot = 1000,
  seed = NULL
)

\S4method{skillScore}{logical,numeric}(
  response,
  predictor,
  reference = NULL,
  threshold = NULL,
  ci = FALSE,
  ciLevel = 0.95,
  nBoot = 1000,
  seed = NULL
)
}
\arguments{
\item{response}{Numeric vector of observed values}

\item{predictor}{Numeric vector of predicted values}

\item{reference}{Reference value for classification}

\item{threshold}{Threshold for classification}

\item{ci}{Logical, calculate confidence intervals via bootstrap (default=FALSE)}

\item{ciLevel}{Confidence level for intervals (default=0.95)}

\item{nBoot}{Number of bootstrap samples if ci=TRUE (default=1000)}

\item{seed}{Random seed for bootstrap reproducibility (default=NULL)}
}
\value{
Data.frame with skill metrics and optional confidence intervals
}
\description{
Calculate prediction skill scores for fishery stock assessment models
}
\details{
When ci=TRUE, bootstrap confidence intervals are calculated for AUC, TSS, TPR, and FPR.
The bootstrap method resamples the data with replacement to estimate the sampling
distribution of each metric. Confidence intervals are added as additional columns:
AUC_CI_lower, AUC_CI_upper, TSS_CI_lower, TSS_CI_upper, TPR_CI_lower, TPR_CI_upper,
FPR_CI_lower, FPR_CI_upper.
}
\examples{
# Basic usage
response = rlnorm(100, meanlog=log(1), sdlog=0.5)
predictor = response * exp(rnorm(100, sd=0.3))
skillScore(response, predictor, reference=1)

# With confidence intervals
skillScore(response, predictor, reference=1, ci=TRUE, nBoot=1000, seed=123)
}
