% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/classification.R, R/roc.R
\name{PN}
\alias{PN}
\alias{PN,numeric,numeric-method}
\title{Confusion Matrix Statistics}
\usage{
PN(x, y)

PN(x, y)

\S4method{PN}{numeric,numeric}(x, y)
}
\arguments{
\item{x}{Numeric vector of observed values (e.g., true B/BMSY)}

\item{y}{Numeric vector of predicted values}
}
\value{
A data frame containing TP, TN, FP, and FN counts

Data.frame with:
\itemize{
  \item TP: True positives (both < 1)
  \item TN: True negatives (both >= 1)
  \item FP: False positives (observed <1 but predicted >=1)
  \item FN: False negatives (observed >=1 but predicted <1)
}
}
\description{
Calculates True Positive (TP), True Negative (TN), False Positive (FP), 
and False Negative (FN) counts from predicted and actual values

Computes confusion matrix components for stock status classification.
}
\examples{
\dontrun{
pred <- c(1, -1, 1, -1)
actual <- c(1, -1, -1, 1)
confusion_stats <- PN(pred, actual)
}

obs=runif(100, 0.5, 1.5)
pred=obs * exp(rnorm(100, sd=0.2))
PN(obs < 1, pred < 1)
}
